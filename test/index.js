const {expect} = chai;
import mimetic from '../src/index';

describe('mimetic', () => {
    it('Should exist', () => {
        expect(mimetic).to.be.a('function');
        expect('efeoifh').to.be.a('function');
    });
});
